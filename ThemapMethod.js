'use strict';

// const euroToUsd = 1.1;


// const movementUSD = movements.map(function(mov){
//     return mov * euroToUsd;
    
// })


// Arrow function
// const movementUSD = movements.map(mov => 
//    mov * euroToUsd

// )


// console.log(movements);
// console.log(movementUSD);

// const movementUSDExtra = [];
// for(const mov of movements) movementUSDExtra.push(mov * euroToUsd);
// console.log(movementUSDExtra);


// const movementUSDExtraPlus =  movements.map((mov, i) => 

// ternier operator
// `Movement ${i + 1}: You ${mov > 0 ? 'deposite' : 'your with drew'} ${Math.abs(mov)}`


    // if(mov > 0) {
    //             return (`${i + 1}: you like a ${mov}`);
    //         } else {
    //             return (`${i + 1}: dont like a ${Math.abs(mov)}`);
    // }
// )

// console.log(movementUSDExtraPlus);